import { Metadata } from "next";

export const metadata: Metadata = { title: "Donation Failed" };

export default function FailedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
