import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Unauthorized",
};

export default function unauthorizedPage() {

    return (
        <div>

            /Unauthorized
        </div>
    )
}